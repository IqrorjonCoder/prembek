🚀 VERCEL DEPLOY GUIDE

STEP 1: Prepare for Git
========================
1. Xabar qil: API key va chat ID .env faylda saqlangan ✓
2. .gitignore .env faylni ignore qiladi ✓
3. Frontend API endpoint ishlatadi ✓

STEP 2: Initialize Git
========================
```bash
cd x:\projects\prembek\test_all_prem
git init
git add .
git commit -m "Initial commit - Prem_Bek application"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/test_all_prem.git
git push -u origin main
```

STEP 3: Deploy to Vercel
========================
1. Vercel.com ga kirish (GitHub bilan)
2. "New Project" bosish
3. GitHub repozitoriyani tanlash
4. Environment variables qo'shish:
   - TELEGRAM_BOT_TOKEN = 7548520724:AAGSRnqwBeb35JM-6609-4WhLphX9dIBTA4
   - TELEGRAM_CHAT_ID = -1003990120376
5. "Deploy" boshing

STEP 4: Test
========================
- Vercel URLni oching
- Premium/Boost/Gift page test qil
- Telegram guruhda xabar ko'rinishini tekshir

🔐 SECURITY CHECKLIST
========================
✓ API key .env faylda
✓ .env .gitignore da
✓ Frontend API endpoint ishlatadi
✓ Backend /api/send-order router
✓ Environment variables Vercel da set
✓ Debug mode disabled (app.py da)
✓ Requirements.txt dependencies

⚠️ IMPORTANT!
========================
- API key hech kimga bermang!
- .env faylni git ga push qilmang (auto ignored)
- Vercel dashboard da environment variables set qil
- Bot guruhda admin bo'lishi kerak
- Timeout 10 sekund

GIT COMMANDS
========================
# Status check
git status

# Add all changes
git add .

# Commit
git commit -m "Your message"

# Push to GitHub
git push

# Check remote
git remote -v

VERCEL COMMANDS (CLI)
========================
# Install Vercel CLI
npm i -g vercel

# Login
vercel login

# Deploy
vercel

# Check logs
vercel logs

🎉 TAYYOR!
========================
Barcha safety va security o'rnatilgan!
Git va Vercel deploy uchun tayyor!
